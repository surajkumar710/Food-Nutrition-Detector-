<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NutriScan - Food Nutrition Analyzer</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/static/css/style.css">
    <link rel="icon" href="/static/images/logo.png">
</head>
<body>
    <div class="container">
        <header>
            <img src="/static/images/logo.png" alt="NutriScan Logo" class="logo">
            <h1>NutriScan</h1>
            <p>AI-powered food nutrition analyzer</p>
        </header>

        <div class="upload-card">
            <div class="upload-area" id="uploadArea">
                <i class="fas fa-cloud-upload-alt"></i>
                <p>Drag & drop food image or click to browse</p>
                <input type="file" id="fileInput" accept="image/*">
            </div>
            <div class="preview" id="previewContainer">
                <img id="imagePreview">
                <button id="removeBtn"><i class="fas fa-times"></i></button>
            </div>

            <div class="health-options">
                <label for="healthCondition">Health Condition:</label>
                <select id="healthCondition">
                    <option value="general">No specific condition</option>
                    <option value="diabetes">Diabetes</option>
                    <option value="hypertension">Hypertension</option>
                    <option value="weightloss">Weight Loss</option>
                </select>
            </div>

            <button id="analyzeBtn" class="btn-primary" disabled>
                <i class="fas fa-search"></i> Analyze Food
            </button>
        </div>

        <div class="results-card" id="resultsCard">
            <div class="loader" id="loader">
                <div class="spinner"></div>
                <p>Analyzing your food...</p>
            </div>

            <div class="results-content" id="resultsContent">
                <h2 id="foodName"></h2>
                <div class="nutrition-facts">
                    <div class="fact">
                        <i class="fas fa-fire"></i>
                        <span id="calories">--</span>
                        <small>Calories</small>
                    </div>
                    <div class="fact">
                        <i class="fas fa-dumbbell"></i>
                        <span id="protein">--</span>
                        <small>Protein (g)</small>
                    </div>
                    <div class="fact">
                        <i class="fas fa-bread-slice"></i>
                        <span id="carbs">--</span>
                        <small>Carbs (g)</small>
                    </div>
                    <div class="fact">
                        <i class="fas fa-oil-can"></i>
                        <span id="fat">--</span>
                        <small>Fat (g)</small>
                    </div>
                </div>

                <div class="recommendations">
                    <h3><i class="fas fa-heartbeat"></i> Health Recommendations</h3>
                    <ul id="recommendationsList"></ul>
                </div>
            </div>
        </div>

        <footer>
            <p>NutriScan | AI Food Nutrition Analyzer</p>
        </footer>
    </div>

    <script src="/static/js/script.js"></script>
</body>
</html>
